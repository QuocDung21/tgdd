spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_tgdd
    username: postgres
    password: Dung112001
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update

server:
  port: 8080


#spring.thymeleaf.prefix=classpath:/templates/
#spring.thymeleaf.suffix=.html

